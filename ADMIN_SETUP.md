# إعداد حساب المسؤول (Admin Setup)

## الطريقة 1: إضافة البريد الإلكتروني إلى قائمة المسؤولين

1. افتح ملف `.env.local` في المجلد الرئيسي للمشروع
2. ابحث عن السطر `ADMIN_EMAILS=`
3. أضف بريدك الإلكتروني إلى القائمة:
\`\`\`env
ADMIN_EMAILS=your-email@example.com,admin@karkey.com
\`\`\`
4. احفظ الملف
5. أعد تشغيل المشروع:
\`\`\`bash
npm run dev
\`\`\`

## الطريقة 2: تعيين is_admin في قاعدة البيانات

1. افتح **phpMyAdmin** في المتصفح: `http://localhost/phpmyadmin`
2. اختر قاعدة البيانات `karkey`
3. اذهب إلى جدول `users`
4. ابحث عن حسابك (باستخدام البريد الإلكتروني)
5. اضغط **Edit** (تحرير)
6. غير قيمة `is_admin` من `0` إلى `1`
7. احفظ التغييرات

## الطريقة 3: استخدام SQL مباشرة

1. افتح **phpMyAdmin**
2. اذهب إلى تبويب **SQL**
3. نفذ هذا الأمر (استبدل البريد الإلكتروني ببريدك):
\`\`\`sql
UPDATE users SET is_admin = 1 WHERE email = 'your-email@example.com';
\`\`\`

## التحقق من الإعداد

بعد تطبيق أي من الطرق أعلاه:
1. سجل دخول إلى الموقع باستخدام حسابك
2. اذهب إلى: `http://localhost:3000/admin`
3. يجب أن تظهر لك لوحة تحكم المسؤول

## استكشاف الأخطاء

إذا لم تعمل لوحة التحكم:
1. افتح **Console** في المتصفح (اضغط F12)
2. ابحث عن رسائل `[v0]` لمعرفة سبب المشكلة
3. تأكد من أنك مسجل دخول
4. تأكد من أن بريدك الإلكتروني صحيح في `.env.local` أو في قاعدة البيانات
